<Page x:Class="HxPosed.GUI.Pages.Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:HxPosed.GUI.ViewModels"
      xmlns:local="clr-namespace:HxPosed.GUI.Pages"
      xmlns:ctrls="clr-namespace:HxPosed.GUI.Controls"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Dashboard">
    <Page.DataContext>
        <vm:DashboardViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <StackPanel Margin="20">
                <ui:CardAction Margin="0,5,0,12" Click="CardAction_Click" Icon="{ui:SymbolIcon ArrowBetweenUp20}">
                    <ctrls:CardControl Header="Install / Update HxPosed" Subtitle="Installs or updates HxPosed on your computer. Requires restart."/>
                </ui:CardAction>
            </StackPanel>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ui:SymbolIcon FontSize="80" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Symbol="{Binding Icon}"/>
            <Grid Grid.Column="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left">
                    <ui:TextBlock FontTypography="Title" Appearance="Secondary" Text="{Binding StatusText}"/>
                    <ui:TextBlock FontTypography="Subtitle" Appearance="Tertiary" Text="{Binding DescriptorText}"/>
                </StackPanel>

            </Grid>
            
        </Grid>
    </Grid>
</Page>
